# Required starting in Version 2 Phase 2 — not used yet.
# Phase 1's /api/parse-description endpoint currently uses a deterministic
# mock extractor (src/utils/parseDescriptionMock.ts) and reads no env vars.
# Phase 2 will replace that mock with a real LLM call using this key.
#
# Server-side only. Never exposed to the browser, never committed —
# copy this file to .env.local and fill in your own key.
ANTHROPIC_API_KEY=
